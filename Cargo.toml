[package]
name = "fxcode"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
libc = "0.2.189"
image = { version = "0.25.10", default-features = false, features = ["jpeg"] }
signal-hook = "0.3"
termion = "4.0.6"
